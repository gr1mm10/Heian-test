{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "robloxLuau",
            "request": "attach",
            "name": "Attach to Roblox Studio",
            "projectRoot": "${workspaceFolder}"
        }
    ]
}
